pipeline {
  agent {
    label 'docker-agent'
  }

  tools {
    nodejs '18.6.0'
  }

  stages {
    stage('JDK version') {
      steps {
        sh 'javac --version'
        echo "Working directory is:"
        sh 'pwd'
        sh 'node -v'
      }
    }
  }
}
