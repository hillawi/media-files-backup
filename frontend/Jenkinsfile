pipeline {
  agent {
    label 'docker-agent'
  }

  tools {
    nodejs ''NodeJS
  }

  stages {
    stage('JDK version') {
      steps {
        sh 'javac --version'
        echo "Working directory is:"
        sh 'pwd'
        sh 'node -v'
      }
    }
  }
}
