spring:
  web:
    resources:
      add-mappings: false
  mvc:
    throw-exception-if-no-handler-found: true
  config:
    import: "configserver:${SPRING_CONFIG_URI}"
  application:
    name: mfb-rest
server:
  servlet:
    encoding:
      force: true